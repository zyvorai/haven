## Summary

<!-- What changed and why -->

## Test plan

- [ ] `go test ./...`
- [ ] `cd ui/web && npm ci && npm run build` (if UI changed)
- [ ] Docs updated if behavior / CLI / deploy paths changed
- [ ] Commits include DCO `Signed-off-by` (`git commit -s`)
